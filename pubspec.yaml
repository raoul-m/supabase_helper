name: dart_types_supabase
description: Generates Dart models/enums/functions from Supabase PostgreSQL.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.1

dependencies:
  postgres: ^3.0.0  # PostgreSQL client
  args: ^2.4.0       # CLI argument parsing
  path: ^1.8.0       # File system operations

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^5.0.0
  test: ^1.24.0

